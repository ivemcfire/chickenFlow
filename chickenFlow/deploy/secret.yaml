# Do NOT commit with a real API key — create this manually on the cluster:
#   kubectl create secret generic ai-secret \
#     --from-literal=api-key="sk-ant-..." \
#     --namespace chickenflow
#
# The template below is for reference only.
apiVersion: v1
kind: Secret
metadata:
  name: ai-secret
  namespace: chickenflow
type: Opaque
stringData:
  api-key: "REPLACE_ME"
